//output: pigGame.html
html
  head
    link(href="https://fonts.googleapis.com/css?family=Roboto:300,400,700" rel="stylesheet")
    link(rel="stylesheet" type="text/css" href="assets/css/main.css")
  body
    .menu
      button(onClick="resetGame()").newGame Ny runde
      .dice.one
      button(onClick="setDice(rollDice())").rollDice#rollDice Trill terning
      button(onClick="hold(current)").hold Behold
    
    .settingsButton.open
      button(onClick="showSettings()") Innstillinger
    .settings.close
      input(placeholder="Spiller 1")#playerOneName
      input(placeholder="Spiller 2")#playerTwoName
      input(placeholder="Mål")#goal
      button(onClick="setDefault()").submitt Lagre
      button(onClick="closeSettings()") Lukk
      
    
    .grid
      .player.player0.active
        .name.nameOne Spiller 1
        .score.score0 0
        .current.current0 
          span Runde
          p.currentScore 0
          
      .player.player1
        .name.nameTwo Spiller 2
        .score.score1 0
        .current.current1 
          span Runde
          p.currentScore 0
        
      script(src="assets/js/section4/pigGame.js" type="text/javascript")
